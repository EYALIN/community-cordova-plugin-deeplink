# Release Notes

## 1.0.0

- Initial release
- Android: App Links and custom URI scheme support
- iOS: Universal Links and custom URL scheme support
- `getLastDeepLink()` for cold-start deep links
- `onDeepLink(callback)` for runtime deep link events
